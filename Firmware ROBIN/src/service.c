#include "robin.h"
#include <xc.h>

void	service(void)
{
	char	energy;

	//start security timer
	//call adc
	if (energy == 1 && user_water)
		distribute_water();		
	else if (energy == 1 && user_soap)
		distribute_soap();	
	else if (energy == 0)
		//inform
}
